{
  "name": "cypress-cucumber",
  "version": "1.0.0",
  "description": "Cypress-Cucumber-Autotest.",
  "main": "index.js",
  "scripts": {
    "cypress:run": "npx cypress run --browser chrome",
    "test": "cypress open",
    "cypress:report": "node cucumber-html-report.js"
  },
  "keywords": [
    "cypress"
  ],
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^13.0.1",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.1.5",
    "cypress": "^10.9.0",
    "esbuild": "^0.15.9",
    "multiple-cucumber-html-reporter": "^2.0.0"
  }
}
